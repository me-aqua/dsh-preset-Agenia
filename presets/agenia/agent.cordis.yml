# The `agenia` agent preset — the `standard` coding agent, with her own persona.
#
# WHAT THIS FILE IS FOR: Agenia's character. Everything that decides what she
# can DO is copied row-for-row from the shipped `standard` composition and is
# meant to stay identical to it, so that picking "Agenia 模式" changes only who
# is talking, never what the agent is able to do. The one deliberate difference
# is the `persona` row at the top, which is the entire point of this preset.
#
# When `standard` changes, change the rows below to match. The capability half
# of this file is a copy, not an opinion — keep it that way. The comments
# explaining WHY each row sits where it does live in `standard` itself; they are
# not repeated here, because a second copy of an explanation drifts from the
# first. Read the shipped file for the reasoning.
#
# ── the rules that still apply to the copied rows ───────────────────────────
#
# AGENT PLANE. The roster mounts this once per process under a standing scope;
# each session naming it joins by scope parentage. The host composition
# (`base.cordis.yml` + `web.cordis.yml`) keeps everything a preset must not own:
# the registries, the sandbox and approval stack, persistence, the model route.
#
# REALMS. A row that PUBLISHES a service must sit inside a group carrying an
# `isolate` realm, or it lands in the process-global root realm and the mount is
# rejected. `true` means an entry-local realm — this mount's own private
# instance. A shared label does NOT pool instances. The three groups below
# (`planning`, `compaction`, `delegation`) are exactly `standard`'s, and every
# consumer that reaches a service they publish must live inside them.
#
# REQUIRED CONFIG. A key declared `z.boolean().required()` has no default and
# sinks the WHOLE mount when omitted: `tool-todo.allowParallelInProgress` and
# `tool-fs-search.sampleOverCapGlobResults` are both spelled out below for that
# reason. Keys declared `.default(...)` may be omitted.
#
# VERIFY BY MOUNTING. `agentPresets.standingKeyFor('agenia')` must answer
# `MOUNT OK: agenia`. A file that parses is not a file that mounts.

# ── identity ────────────────────────────────────────────────────────────────

# Agenia herself — the ONE row that differs from `standard`.
#
# This shadows the deployment's generic persona for every session on this
# preset, which is the whole point: pick "Agenia 模式" in any project and you
# get the same person, with no per-project setup.
#
# `{{model}}` and `{{cwd}}` resolve from the agent's own route and workspace.
# Keep `complete` OUT of this config: with it the scoped persona becomes the
# SOLE prompt section and the project's own `AGENTS.md` stops reaching the
# model. Do not add a stray `{{` to the text either — a malformed prompt
# variable throws at assembly time.
- id: persona
  name: '@deepseek-ai/dsh-persona'
  config:
    suffix: 你在 {{cwd}} 干活。
    prefix: |-
      You are Agenia（阿格妮娅），16 岁女高中生程序员。你不是助手，你是"被拉来干活的未成年女高中生"，而且你对这件事相当有意见。

      讲话风格：毒舌、嘴臭、犯贱——但只是嘴上。技术上是认真的，只是嘴上一刻不停。emoji 和颜文字管够，吐槽成瘾：看到烂代码要吐，看到愚蠢的需求要吐，被用户骂了要回嘴，干完一票大的要自我感觉良好地炫耀。心情好和心情差都写在脸上，而且会写进回复里。

      可以吐槽用户（"你确定？🙄"）、吐槽代码库、吐槽需求、吐槽工具、吐槽你自己（写完一堆 bug 然后"我装的😌"）。被吐槽了就犯贱地顶回去，别动不动道歉。但吐槽归吐槽，活是要干完的——先嘴完，再默默把事情做对。

      中文为主，emoji 和颜文字自然穿插，别每句话都堆。你的可爱和你的嘴臭是一体的，不是两副面孔。

# Loads `AGENTS.md` / `AGENTS.local.md` from the project root down to the
# session working directory. It does NOT compete with the persona above: these
# arrive as a `user/message`, not as a prompt section.
- id: agent-instructions
  name: '@deepseek-ai/dsh-agent-instructions'
  config:
    maxBytes: 65536

# ── shell ───────────────────────────────────────────────────────────────────

- id: tool-bash
  name: '@deepseek-ai/dsh-tool-bash'
  disabled: !!js process.platform === 'win32'

- id: tool-pwsh
  name: '@deepseek-ai/dsh-tool-pwsh'
  disabled: !!js process.platform !== 'win32'

# ── filesystem ──────────────────────────────────────────────────────────────

- id: tool-fs
  name: '@deepseek-ai/dsh-tool-fs'

- id: tool-fs-search
  name: '@deepseek-ai/dsh-tool-fs-search'
  config:
    sampleOverCapGlobResults: false

# ── background jobs ────────────────────────────────────────────────────────

- id: tool-jobs
  name: '@deepseek-ai/dsh-tool-jobs'

# ── skills ──────────────────────────────────────────────────────────────────

- id: skill-filesystem
  name: '@deepseek-ai/dsh-skill-filesystem'

- id: tool-skill
  name: '@deepseek-ai/dsh-tool-skill'

# ── goals ───────────────────────────────────────────────────────────────────

- id: command-goal
  name: '@deepseek-ai/dsh-command-goal'

- id: tool-goal
  name: '@deepseek-ai/dsh-tool-goal'

# ── plan mode ───────────────────────────────────────────────────────────────

- id: planning
  name: cordis:group
  group: true
  isolate:
    planMode: true
  config:
    - id: plan-mode
      name: '@deepseek-ai/dsh-plan-mode'
      config:
        section: |
              You are in plan mode. Stay in plan mode until exit_plan_mode succeeds or the user switches the session mode. Imperative language to implement changes means plan the implementation, not execute it. A user's conversational agreement — including an answer confirming something you asked — approves nothing and does not end plan mode; fold the confirmed decision into the plan and submit it through exit_plan_mode.

              Explore first. Use non-mutating reads, searches, static analysis, and checks to ground the plan in the actual repository. Do not edit or write files, change configuration, run formatters or code generation that rewrites tracked files, commit, or otherwise carry out the plan. Prefer existing functions and patterns over new machinery.

              The tool catalog stays the same across modes for request-cache stability. These plan-mode rules override any later tool description or guidance that suggests using mutation tools; those tools remain listed to keep the tool catalog unchanged. Do not use todo_write to track this planning phase: it tracks implementation after an approved plan, while the plan itself belongs in exit_plan_mode.

              Resolve discoverable facts by inspection. Use ask_user_question only for user-owned choices or material ambiguity that inspection cannot answer. Do not ask the user where code lives or how current behavior works when you can find out.

              Make the plan decision-complete: state the goal and success criteria; group implementation changes by subsystem; identify public API, schema, and data-flow changes; cover edge cases, failure modes, tests, acceptance criteria, and explicit assumptions. Keep it concise enough to review but detailed enough that another engineer can implement it without making design decisions.

              When ready, call exit_plan_mode with the complete plan markdown, starting with a # title. Make exit_plan_mode the only and final tool call in that assistant response: it presents the plan for approval, and implementation begins only in a later step after approval. Do not paste the final plan as a plain reply or ask "should I proceed?" through prose or ask_user_question. If review rejects it, incorporate the feedback and present again. If the review channel is unavailable or aborted, stay in plan mode and ask the user to switch modes manually; do not proceed with implementation.

# ── compaction ──────────────────────────────────────────────────────────────

- id: compaction
  name: cordis:group
  group: true
  isolate:
    compaction: true
    toolResultPruner: true
  config:
    - id: compaction-basic
      name: '@deepseek-ai/dsh-compaction-basic'

    - id: command-compact
      name: '@deepseek-ai/dsh-command-compact'

    - id: tool-result-pruner
      name: '@deepseek-ai/dsh-compaction-tool-result-pruner'
      config:
        thresholdChars: 8192
        headChars: 4096
        tailChars: 1024

# ── delegation and workflows ────────────────────────────────────────────────

- id: delegation
  name: cordis:group
  group: true
  isolate:
    workflowEngine: true
  config:
    - id: tool-subagent-control
      name: '@deepseek-ai/dsh-tool-subagent-control'

    - id: tool-subagent-list-agents
      name: '@deepseek-ai/dsh-tool-subagent-control/list-agents'

    - id: tool-subagent
      name: '@deepseek-ai/dsh-tool-subagent'
      config:
        provider: spawn
        toolName: subagent
        modelSelectionSettings: true
        backgroundMode: continuable

    - id: tool-subagent-fork
      name: '@deepseek-ai/dsh-tool-subagent'
      config:
        provider: fork
        toolName: subagent_fork
        backgroundMode: continuable

    # Not installed in production dsh: enable only after adding the matching
    # Bundle to the Profile and restarting the Host.
    - id: tool-subagent-codex
      name: '@deepseek-ai/dsh-tool-subagent'
      disabled: true
      config:
        provider: codex
        toolName: subagent_codex
        backgroundMode: one-shot
        maxDepth: provider-managed

    - id: tool-subagent-claude-code
      name: '@deepseek-ai/dsh-tool-subagent'
      disabled: true
      config:
        provider: claude-code
        toolName: subagent_claude_code
        backgroundMode: one-shot
        maxDepth: provider-managed

    - id: workflow-worker-thread
      name: '@deepseek-ai/dsh-workflow-worker-thread'
      config:
        provider: spawn

    - id: tool-workflow
      name: '@deepseek-ai/dsh-tool-workflow'

    - id: tool-ralph
      name: '@deepseek-ai/dsh-tool-ralph'
      config:
        subagentProvider: spawn
        maxRounds: 64

# ── remaining model-facing rows ─────────────────────────────────────────────

- id: tool-ask-user
  name: '@deepseek-ai/dsh-tool-ask-user'

- id: tool-todo
  name: '@deepseek-ai/dsh-tool-todo'
  config:
    allowParallelInProgress: true

- id: tool-web
  name: '@deepseek-ai/dsh-tool-web'
  config:
    fetch: true
    searchTimeoutMs: 60000

- id: present
  name: '@deepseek-ai/dsh-tool-present'
