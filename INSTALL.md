# 装上 Agenia —— 长参考

> **这份文件是给"别人"看的**：一个没参与过这个仓库、手上只有它的人。
> **最短路径在 [`README.md`](README.md) 的「安装」那一节** —— 三步：拷文件夹 → **不用关 DSH** → 新开一个会话选它。
> 本文是同一件事的长版本：每一步**为什么**这么做、怎么确认成了、卡住了怎么查。
>
> **本文的动作在 Windows 上实测过（2026-09-20）**；macOS / Linux 上把路径换成
> `~/.dsh/.agent-presets/` 即可。

---

## 1. 这件事有多简单

**把仓库里的 `presets\agenia` 这个文件夹，整个拷进 `%USERPROFILE%\.dsh\.agent-presets\`。**

就这样。没有命令行、没有安装步骤、没有要手写的配置文件。

**为什么不用装任何东西**：那份预设的插件行写的是**相对路径**（`./inject.js`），
而相对路径按**声明它的那份组合文件自己的目录**解析 —— 也就是 `agenia` 文件夹本身。
插件文件就躺在那个文件夹里，于是**文件夹拷到哪，插件就跟到哪**。
（写成裸包名才会按 profile 目录解析，那就得先往 profile 里装一个包；这条已经不用了。）

**为什么不用改配置**：DSH 找预设只认三个地方 —— 随它自带的（只读）· 配置里登记的目录 ·
**`~/.dsh/.agent-presets/`（用户根，默认就开着）**。我们走第三个，所以不需要任何登记。

---

## 2. 拷之前先确认两件事

| 要有 | 怎么确认 | 成了长这样 |
| --- | --- | --- |
| 这个仓库 | 拿到 `presets\agenia` 这个文件夹 | 进去第一眼就是 `agent.cordis.yml`、`inject.js`、`persona.md`、`team\` |
| 一个能跑的 DSH | 起一个会话 | 输入框上方那一格显示着当前预设的名字（默认「标准模式」） |

⚠️ **要拷的是 `agenia` 这一层，不是它外面那层。** 判据很直白：
**拷完进去，第一眼必须看到 `agent.cordis.yml`。** 如果你看到的是另一个 `agenia\`，那就多了一层。

---

## 3. 拷到哪、怎么拷

在资源管理器地址栏里粘：

```
%USERPROFILE%\.dsh\.agent-presets
```

回车。**没有这个文件夹就自己建一个** —— 名字必须**正好**是 `.agent-presets`
（最前面那个点是名字的一部分）。然后把 `agenia` 整个拖进去。

拷完长这样：

```
%USERPROFILE%\.dsh\.agent-presets\
└─ agenia\
   ├─ agent.cordis.yml
   ├─ preset.yml
   ├─ package.json        (name 和 version 都必须有)
   ├─ inject.js           (唯一的代码文件)
   ├─ persona.md  leader.md  work-guidelines.md  说明.md
   └─ team\                (design / dev / test / review / retro / hire)
```

**三种"看起来装上了、其实 DSH 完全看不见"的拷法**（都不报错，只是菜单里永远没有它）：

| 拷成了这样 | 为什么看不见 |
| --- | --- |
| `agenia\agenia\agent.cordis.yml` | 多了一层 —— 扫描器找的是**正下面**那一层里的 `agent.cordis.yml` |
| `agenia - 副本\` / `agenia (1)\` / `agenia 副本\` | 目录名必须匹配 `[a-z0-9][a-z0-9-]*`，不合法的名字**直接跳过**（第二次粘贴时最容易撞上这条） |
| 图标上带小箭头的"文件夹"（快捷方式 / 目录联接） | Node 把 Windows 的目录联接报成符号链接、不是目录，而扫描器只收真目录。**必须是真拷贝** |

---

## 4. 拷完要不要重启？不用

**拷完就完事了 —— 正在跑的那个 DSH 不用停、也不用重启。** 用户根那个目录是**每次现扫**的：
实测（本机 Windows，2026-09-20）—— 同一个 harness 进程、**没重启**，运行中往
`~/.dsh/.agent-presets/` 写进去一份新预设，**26 秒后**名单里就有它了（5 条 → 6 条）。

**新开一个会话**（第 5 步）那一格里就应该出现「Agenia 模式」。

**万一一时没出现：重启一次就好** —— **重启 = 先停掉现在这个，确认它退了，再重新起一个。**
这是**兜底**，不是装的时候必须先做的事。

⚠️ **别直接再开一个**：两个实例会抢 `~/.dsh/.credentials.yaml` 的写锁，第二个会报
`atomic-write: timed out waiting for the writer lock`。

---

## 5. 怎么知道成了

新开一个会话 → 点输入框上方写着当前预设的那一格 → 在列表里选「**Agenia 模式**」→
**问她三件事，一件对一样**：

**① 问「你是谁，手底下有几个人」。**
她自称 16 岁的组长，报得出名字和五个岗位（设计与美工 / 实现 / 测试 / 评审 / 复盘）。
⇒ **人格与名册注入通了。**

**② 让她派评审去改一个源码文件。**
评审应该拒绝，报文里带 `你的岗位（review）只能写项目里 .team/ 下面的东西`。
⇒ **写边界生效了。**（另一条边界也顺带验了：评审连命令行都没有。）

**③ 说「开工」。**
她先读 `.team/leader/now.md`，并报一句 `.team/retro/<今天>/` 里有没有 `每日复盘.md`。
⇒ **工作位置那套规矩进去了。**

> **想知道你这一份到底是哪一个目录里的？** 问她：**"岗位说明书在哪个目录？"**
> 名册那一段里带着绝对路径。报出来不是 `\.dsh\.agent-presets\agenia` 就说明**跑的是另一份**
> —— 见第 8 步末段（旧预设根登记还没删）。

**三种没成，长得不一样，别混：**

| 你看到的 | 是什么 | 去哪查 |
| --- | --- | --- |
| 那一格里**根本没有**「Agenia 模式」 | 预设**根本没被发现** | 回第 3 步那张表（层级 / 名字 / 联接），再按第 4 步重启一次（兜底） |
| 有，但**点不动 / 是灰的 / 旁边带警告** | 组合文件被找到了、**挂载失败** | 多半是插件行解析不到：`inject.js` 在不在预设根目录？`agent.cordis.yml` 里那一行还写着 `./inject.js` 吗？ |
| 能切过去，但它说自己是"一个编码 agent" | **人格没注入** | 先按第 5 步问一句"岗位说明书在哪个目录"，确认跑的是哪一份；`inject.js` 语法坏过一次的话，**重启 harness**（第 7 步） |

⚠️ **第二种要连发两条消息再下结论**：插件出问题不一定在加载期报，也可能**每次请求**才报
（错误里含 `REQUEST_EXTENSION`）。第一条正常不等于第二条正常。

---

## 6. 装了之后怎么改

**九份 markdown 直接改，存盘即生效** —— 正在跑的会话下一个步骤就变，不用重启：

| 你想改的 | 改哪 |
| --- | --- |
| 人格 / 带队方式 / 工序 / 岗位说明书 | `%USERPROFILE%\.dsh\.agent-presets\agenia\` 下的那九份 `.md` |
| 组合本体（能力行 / 注入顺序 / 谁受限） | 同一个目录里的 `agent.cordis.yml` —— ⚠️ 这个要**重启** |

> 哪九份、谁读哪一份，见 [`README.md`](README.md) 的「文件」一节。
> 每个文件为什么长这样，见预设文件夹里的 `说明.md`。

⚠️ **更新这份预设 = 把新版本的文件夹再拷一遍覆盖。** 覆盖之后，你手改过的那几份 `.md`
会**一起被覆盖掉** —— 想留就先备份。

⚠️ **她会在你的项目里建一个 `.team/`** （队伍的办公桌：短期记忆 / 长期记忆 / 每天的工作抽屉）。
那是她的东西，**留着别删 —— 那是"怎么做的"的档案**（2026-09-24 老板：「没人会删的」） —— 建议把它加进 `.gitignore`。

---

## 7. 改注入器的代码，必须重启

`inject.js` 是**代码**：ESM 按 URL 缓存，改它不会自动重新加载。
而且**"导入失败"也会按 URL 缓存** —— 它曾经一度处于语法错误状态，之后无论怎么修都一直
复用坏掉的那一份，症状是**挂载正常、行状态正常、人员静默消失**。

**唯一管用的办法是重启 DSH。** 那一行写的是**预设相对路径** `./inject.js`，
没有版本号、也没有查询串 —— 所以改 `package.json` 的 `version` 换不出新 URL。
那九份 `.md` 不受影响（每轮从磁盘读）。

改完先做一次语法检查（在仓库里跑；`<node>` 换成你机器上的 node）：

```sh
<node> --check presets/agenia/inject.js
```

⚠️ **别让它带着语法错待着** —— 坏的那次导入会被缓存住，之后怎么改都不生效，而且不报错。

---

## 8. 不想要了

把 `%USERPROFILE%\.dsh\.agent-presets\agenia` 这个文件夹删掉，**就完了** ——
它没往别的地方装过任何东西，也没有要还原的配置文件。（想让菜单里立刻没有它，
重启一次 —— 同第 4 步，那是兜底。）

⚠️ **如果你这台机器上还留着旧的预设根登记**（更早的装法留下的，例如配置里的 `roots` 指向仓库），
那是另一回事 —— 而且**它会让这份新拷贝不生效**。名册是**按 id 去重的，排在前面那个根赢**，
顺序是「随 DSH 自带的 → 配置里登记的 → 用户根」：

⇒ **旧登记还在时，你拷进用户根的这份不会被选中**，跑的是旧目录里那份。
菜单里**不会**出现两条「Agenia 模式」，只会有一条 —— 所以别靠"看到两条"来判断。
判据是第 5 步那句"岗位说明书在哪个目录"：报出来是旧路径 ⇒ 去把旧的 `roots` 登记删掉
（改 profile 的 `cordis.patch.yml` 必须在 **DSH 关着**的时候做），再重启。

---

## 9. 踩过的坑（每条都是实测，不是提醒）

| 现象 | 真实原因 | 怎么办 |
| --- | --- | --- |
| 拷完新开的会话里没有它（重启也没有） | 多了一层 / 目录名带了"副本" / 建的是目录联接 | 回第 3 步那张表 |
| 从第二条消息起报 `REQUEST_EXTENSION` | 插件目录的 `package.json` 缺 `name` 或 `version` | 两个字段都必须有（这条在仓库历史上真踩过） |
| 改了 `inject.js`，怎么都不生效，而且不报错 | ESM 按 URL 缓存，**导入失败也缓存** | **重启 DSH**（第 7 步），没有第二条路 |
| 改了某份 `.md` 却看不到变化 | 改的可能是**另一份** Agenia 里的同名文件 | 按第 5 步问一句"岗位说明书在哪个目录" |
| 第二个实例起不来，报 `atomic-write: timed out waiting for the writer lock` | 两个实例在抢同一把写锁 | 重启 = 先停旧的（第 4 步）。停在半路会留下僵锁：删掉 `~/.dsh/.credentials.yaml.lock` |
| 用目录联接把预设"接"进 `.agent-presets\`，DSH 看不见 | Node 报 `isDirectory=false, isSymbolicLink=true`，扫描器跳过 | 拷一份真的进去 |
| `--check` 报语法错 | 你改了 `inject.js`，而且它现在真的坏了 | 先改回来。⚠️ 坏着的时候别指望"重启一下就好" —— 坏的那次导入会被缓存 |
| 评审说它"写不了"，但你其实想让它写 | 那是**设计如此**：评审只能写 `.team/` 下的东西 | 要改什么，让它写成意见交回组长 —— 这就是"只审不改"的意义 |

---

## 10. 这份文件的口径与日期

**2026-09-20 改版（两次）。**

- **第一次**：这份文件以前教的是另一条路：把注入器当成一个 npm 包装进 profile 的
  依赖里，再手写 profile 的一份补丁文件去登记一个预设根。**那条路已经不用了** ——
  插件改成跟着预设文件夹走（见第 1 节），所以本文现在只讲"拷文件夹"这一条。
- **第二次**：删掉了「装完必须重启」。实测用户根是**每次现扫**的（见第 4 节），
  **重启现在只是兜底**。
- **第三次（同日，随预设换版）**：插件从 `persona-plugin/inject.js` 挪到 **`inject.js`**
  （直接住预设根目录）；验收入口从"问她内容目录"改成**三件事**（第 5 节）；
  被注入的 markdown 从十二份变成**九份**。

**实测过的是**（都是本机、Windows）：目录名必须匹配 `[a-z0-9][a-z0-9-]*`（扫描器的正则）·
目录联接会被静默跳过 · `~/.dsh/.agent-presets/` 是默认打开的用户根 ·
相对路径的插件行按**预设文件夹**解析 · 插件目录的 `package.json` 缺 `version`
会在**每次请求**时报 `REQUEST_EXTENSION` · 改 `inject.js` 只能靠重启 ·
**用户根那份名单是每次现扫的**（运行中写进去的新预设，同一个进程 **26 秒后**就进了名单
—— 所以装完不用重启）· 写边界的两半都拦得住（shell 与落点）。
**换版之后的真机复测**：名册 / 挂载 / 注入顺序 / 受众分流 / 写边界 / 开工第一步，
六项全过 —— 证据在仓库的 `.tools/mount-test/evidence/` 下。

**没验过的**：macOS / Linux 上的表现（本文的动作只在 Windows 上跑过）。
